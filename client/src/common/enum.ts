export enum EnumDialogType {
    DialogStock,
    DialogNotification,
    DialogJournal
}

export enum EnumColumnType {
    Text,
    Date,
    DateTime,
    Number,
    Link,
    Image,
    Tag,
    SignedTag,
    Boolean,
}

export enum EnumFormatType {
    None,
    Text,
    Date,
    DateTime,
    Timestamp,
    Currency,
    Percentage,
    Number,
    AbsPercentage
}
