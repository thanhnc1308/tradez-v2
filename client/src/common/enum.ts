export enum EnumDialogType {
    DialogStock,
    DialogNotification,
    DialogJournal
}

export enum EnumColumnType {
    Text,
    DateTime,
    Number,
    Link,
    Image,
    Tag,
    SignedTag,
    Boolean,
}

export enum EnumFormatType {
    None,
    Text,
    Date,
    Timestamp,
    Currency,
    Percentage,
    Number,
    AbsPercentage
}
